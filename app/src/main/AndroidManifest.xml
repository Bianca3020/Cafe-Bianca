<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.biancaputri.pos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Kasir">
        <activity
            android:name=".viewmodel.DataProdukViewModel"
            android:exported="false" />
        <activity
            android:name=".model.ModelProduk"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".kategori.DataKategoriActivity" />
        <activity android:name=".kategori.ModKategoriActivity" />
    </application>

</manifest>